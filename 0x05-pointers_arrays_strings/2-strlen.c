#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 * _strlen - a function
 * @s: a manne
 * Return: zero
 */
int _strlen(char *s)
{
	unsigned int y;

	for (y = 0; s[y] != '\0'; ++y)
	{
	}
	return (y);
}
