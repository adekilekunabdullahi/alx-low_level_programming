#include <stdio.h>
#include <string.h>
/**
 *main - Entry point
 *
 *Return: Always 0 (success)
 */
int main(void)
{
	char str1[52];
	strcpy(str1, "\"programming is like building a multilingual puzzle");
	puts(str1);
	return (0);
}
