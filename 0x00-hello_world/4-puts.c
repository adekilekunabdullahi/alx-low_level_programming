#include <stdio.h>
#include <string.h>
/**
 *main - Entry point
 *
 *Return: Always 0 (success)
 */
int main(void)
{
        char s[];

        strcpy(s, "\"programming is like building a multilingual puzzle");
	puts(s);
	return (0);
}
